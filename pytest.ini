[pytest]
testpaths = tests
pythonpath = plugins dags
log_cli = true
log_cli_level = WARNING
markers =
    unit: Pure unit tests — no Airflow DB, no network
    dag_integrity: DAG structure / import tests — requires Airflow DB init
